# Use the latest Ubuntu image as the base
FROM ubuntu:latest

# Update APT and upgrade the installed software
RUN apt-get update && apt-get upgrade -y

# Print "Hello, World!" when the container starts
CMD ["echo", "Hello, World!"]

